{
  "name": "disklocation-next",
  "version": "0.0.0",
  "private": true,
  "description": "Standalone TypeScript service that maps Unraid drives to a tray/bay layout via the unraid-api GraphQL interface",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^20.14.0"
  }
}
